apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
#only a single pod can be created with above manifest
# to create multiple pods, we can use a deployment